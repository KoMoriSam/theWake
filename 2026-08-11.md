---
title: Web 实现分页阅读器的一种方式（Vue3）
aliases:
  - 移动端阅读器
  - Web 分页阅读
  - CSS 多栏布局
date: 2026-08-11T00:00:00.000Z
tags:
  - 开发/记录
banner: css.jpg
created: 2026-08-11T08:08:31.000Z
modified: 2026-08-11T09:15:17.000Z
public: true
---

> [!warning]  
> 在本文中，你将看到包括但不限于以下内容：
> - 烂梗云集（云集在哪？）
> - AI 风味遣词造句
> - 屎山代码
> - 神人站长

OK 啊家人们，我也是直接一个造轮子好吧，给本站的小说阅读器做了一个移动端大适配就是说。终于可以在手机上左右翻页了喵～

---

## 都是分页器我没想降维打击！

### `splitMarkdown`——~~分页~~切割器 1.0

旧版阅读器会先调用 `splitMarkdown`，把章节内容拆成若干段，再把每一段当作一页。这套逻辑并不知道读者的屏幕有多高，也不知道当前字号、行高和段间距。手机换个尺寸，或者用户调大两个字号，同一份 Markdown 仍然会得到相同的切分结果。

于是，“一页”只是代码里的一个数组元素。它可能刚好占满屏幕，也可能只有短短一段。读者需要先把这一页滚到底，再寻找上下页或者上下章按钮。如果继续沿着这条路修改，就得在 Markdown 渲染前估算每段文字的高度。图片、引用块、列表、代码块和脚注还会让估算结果继续漂移。

所以，让分页发生在 Markdown 已经变成 DOM 之后才是正解。浏览器负责换行，代码只负责计算每一栏有多大，以及当前应该显示哪一栏。这样一来，页码会随着屏幕和排版设置改变，原来包含页码的段落 ID（`{uuid}-{page}-{段落序号}`）已经不再适用于新的分页机制，直接 `{uuid}-{段落序号}` 即可。

### CSS 多栏布局——分页器 2.0

> [!note]  
> CSS 多栏布局并不是什么新鲜技术，很久之前网络上就有这样的实现分享，至于为什么我没有这样做，原因之一是我懒、之二就是当时的 AI 还没有这么能干，~~我俩联手~~做出来的分页效果实在看不下去。

步入正题，浏览器其实已经提供了一套接近分页的排版方式：CSS Multi-column Layout。给文章设置固定宽高，再把栏宽设成页面宽度，正文就会从第一栏流向第二栏、第三栏：

```css
.mobile-page-article {
  height: var(--reader-page-height);
  width: var(--reader-page-width);

  column-width: var(--reader-page-width);
  column-gap: var(--reader-page-gap);
  column-fill: auto;

  overflow: visible;
}
```

阅读器的视口负责隐藏其余栏。当前页改变时，不需要重新渲染 Markdown，只要把整篇文章向左移动：

```css
.mobile-page-article {
  transform: translate3d(
    calc(var(--reader-chapter-offset, 0px) - var(--reader-page-offset)),
    0,
    0
  );

  transition: transform 220ms cubic-bezier(0.2, 0.8, 0.2, 1);
}
```

其中：

```js
const pageStride = pageWidth + pageGap;
const pageOffset = (currentPage - 1) * pageStride;
```

这样得到的每一页仍属于同一棵 DOM。段落 ID、段评按钮、脚注引用和书内搜索锚点都不用跟着分页器复制一遍。

页数计算倒是踩了一个坑。部分移动浏览器在多栏布局中返回的 `scrollWidth` 只包含第一栏，直接用它除以页面宽度，有时整章会被算成一页。

现在分页器会找到正文的最后一个字符，通过 `Range.getClientRects()` 获取它所在的位置，再换算成最终页码：

```js
const lastTextPage =
  Math.floor(Math.max(0, lastTextLogicalLeft) / pageStride) + 1;

if (lastTextRect) return Math.max(1, lastTextPage);
```

末尾定位点只在正文没有可测量文本时兜底。这样还能避开定位点单独掉进新栏、凭空多出一张空白末页的问题。

#### 一页的高度

页面高度能不能不能直接写成 `100vh`？如果你没有其他元素的干扰，只想做全屏纯阅读，那当然可以。但是我的网站有底部的 dock 栏，浏览器还有 `visualViewport`。移动端地址栏伸缩、横竖屏切换和软键盘都会改变实际可见区域。

所以，当前实现会读取：

- 阅读视口顶部；
- `visualViewport` 的底部；
- dock 控制栏的顶部；
- 阅读器和 dock 之间预留的空隙。

然后取出正文真正能使用的高度：

```js
const availableHeight = Math.floor(
  Math.min(dockTop, visualBottom) -
    viewportTop -
    PAGE_DOCK_GAP,
);
```

有了高度还不能立刻开始分页。如果直接使用用户设置的行高，页面底部通常会剩下一小截空间。不同页面的首行位置相同，末行却可能忽高忽低。

分页器会先计算当前空间能容纳多少个完整行高：

```js
const lineCount = Math.max(
  1,
  Math.floor(contentHeight / preferredLineHeight),
);
```

再让这些行恰好填入正文区域：

```js
const pageLineHeight = contentHeight / lineCount;
```

这里调整的幅度通常很小，但每页首行和末行会落在同一组上下基线上。

由于技术原因，目前的阅读器暂时存在不能任意调整段间距的问题。我这里把段间距暂时先量化为半行高的倍数：

```js
const paragraphGap =
  paragraphGapHalfSteps * (pageLineHeight / 2);
```

> [!note]  
> 事实上，当前版本的排版系统是没有做完全的，现在只有整数倍行高才能够做到首末行两端对齐。

如果脚注、引用块或其他块级元素留下了不足一行的尾部空间，分页器会收集本页已有的块间距，把缺少的高度平均分配进去。调整后还要重新检查页数和脚注归属；只要其中一项发生变化，这次校正就会撤销。

章节头部则作为一个完整块处理。先测量整个 `ChapterHeader` 的高度，再把“头部加正文前间距”的总高度补到行高倍数。标题、字数和更新时间仍然使用桌面端组件，不会各自对齐后把头部拆得七零八落。

#### 尾注？脚注？

桌面端可以把尾注统一放在文章末尾。移动端左右翻页时，如果脚注仍然留在章节最后，读者看到引用标记后还得翻很多页才能找到说明。

所以移动端会先隐藏 Markdown 原本生成的尾注区，再扫描正文中的 `.footnote-ref`，按照引用标记所在的栏，把脚注分配到对应页面。没有脚注的页面不预留空间。有脚注时，阅读器会在屏幕外渲染同一个 `PageFootnotes` 组件，测出实际高度：

```js
measuringFootnotes.value = notes;

await nextTick();
await waitForLayout();

const measuredHeight = footnoteMeasureRef.value?.scrollHeight || 0;
```

脚注区沿用桌面端的分隔线、序号和正文样式，只压缩了移动端的边距。高度按测量结果直接保留，同时保证页面至少还能放下三行正文。

新的问题也来了：浏览器已经把正文排满整页，脚注突然占掉底部空间时，最后几行会和脚注重叠。如果直接把整个段落推到下一页，前一页可能空掉一大块。现在的处理方式是从正文文本中寻找第一枚越过脚注顶部的字符：

```js
const hasOverflowingRect = Array.from(
  range.getClientRects(),
).some((rect) => rect.bottom > footerTop);
```

找到切点后，分页器会把后半段提取成一个续段，并要求它从下一栏开始：

```css
.mobile-footnote-continuation {
  margin-block-start: 0;
  break-before: column;
}
```

DOM 拆段会让浏览器误以为前半段已经结束，所以前半段的末行需要继续两端对齐：

```css
.mobile-footnote-split-source {
  text-align-last: justify;
}
```

如果切点正好落在段首，搬到下一页的是完整段落，首行仍然保留空两格。真正的续写片段则取消缩进。

脚注排版结束后还要重新统计页数，因为插入强制换栏点会继续推动后面的内容。整个过程允许多轮执行，直到每页脚注都有空间，或者达到布局次数上限。

#### 关于空白页 BUG

字号、行高、段间距、屏幕尺寸和字体加载状态都能触发重新分页。一次分页还包含多个异步阶段：

1. 等待 Vue 更新 DOM；
2. 等待浏览器完成布局；
3. 测量页数；
4. 收集脚注；
5. 测量脚注高度；
6. 插入换栏点；
7. 校正页面基线；
8. 恢复阅读位置。

如果用户在中途再次调整字号，旧任务可能拿着上一套行高继续修改新的 DOM。最后得到的页面会混合两次测量结果，空白首页也就出现了。

现在每次测量都会生成一个 token。新的排版请求会立即让旧 token 失效：

```js
const token = ++measureToken;

if (token !== measureToken) return;
```

正在执行的任务没有完成时，下一次请求只会被记入队列，等当前任务退出后再运行。旧任务拆开的脚注段落、强制换栏点和基线补偿也会统一回滚，不能留在页面里。

提交测量结果前，分页器还会检查第一页和当前页是否真的存在可见正文。如果检测到空白页，会清除临时排版并重新测量；连续失败后，才会寻找离当前页最近的有效页面作为兜底。

> [!note]  
> 即使是这样，还是有概率复现页面不稳定出现空白首末页的情况。所以这个 bug 仍然有待修复。

同时，移动设备的软键盘也会改变 `visualViewport.height`。页码输入框或者书内搜索获得焦点时，分页器暂时冻结；输入框失焦、视口恢复稳定后再重新测量。这个冻结只处理网页里的输入框。

> [!note]  
> 浏览器地址栏是否获得焦点没有可靠的网页事件，把所有视口变化都当成键盘又会破坏正常的屏幕自适应。因此，当用户**在网页之外**唤出软键盘，或执行其他能够改变视口的操作时，分页器并不会被冻结。
> 
> 也许有更好的方法稳定分页，但是目前我和 AI 都还没有想到。

#### 翻页和点击操作

如果左右点击区域覆盖太大，读者点击段评按钮时也可能触发翻页。现在只有页面左右各 18% 的区域响应轻触翻页，中间部分留给正文选择、链接和段落评论。横向滑动超过 44 像素后，也可以翻到前后页。以下这些元素会直接排除在翻页手势之外：

```text
a
button
input
select
textarea
summary
contenteditable
data-paragraph-id
```

段评按钮周围还额外增加了 24 像素的保护范围。手指落在按钮附近时，阅读器不会抢走点击。

> [!note]  
> 实际体验上，用手指左右滑动翻页存在触发不灵的问题，所以目前版本的阅读器推荐使用 dock 栏按钮或点击左右侧翻页。

到了章节边界，翻页动作会先保存当前页面快照。新章节加载并完成分页后，旧快照和新页面按照原方向滑动。于是，本章末页翻到下一章首页仍然是向左滑，章节首页翻回上一章末页则向右滑，从而实现无缝切换章节。

但是，由于章节加载机制，在加载期间将覆盖一层轻微模糊效果的遮罩，并暂停翻页和控制按钮，避免读者在章节数据尚未准备好时连续操作。

#### 底部 Dock 栏

移动端 Dock 栏有以下操作：

- 使用帮助；
- 上一页或上一章；
- 当前页数与全书阅读进度；
- 下一页或下一章；
- 评论。

点击中间的页数会打开统一的阅读控制 modal。里面包含左右翻页和上下滚动切换、书内搜索、页码跳转、返回封面、章节目录、排版设置和刷新内容。

上下滚动模式直接使用正常的 Markdown 文章流排版。滚动容器的上下边界也会避开 dock，切换模式时再通过段落锚点恢复到接近原位置。

从页面顶部下滑可以调出导航栏；在导航栏区域上滑可以手动收起。导航菜单展开时会暂停自动隐藏计时，免得用户点菜单点到一半，整条导航栏自己跑了。

## 一句话总结：基本可用，但 BUG 不少

### 分页结果看命

CSS 多栏省掉了手写换行算法，但不同浏览器对多栏、复杂块元素和字体加载的处理仍可能存在差异。目前已经绕过了部分浏览器 `scrollWidth` 不完整的问题，更多设备还需要继续实测。

### 只会处理简单文字流

现在主要针对正文段落、引用标记和普通尾注排版。特别高的表格、代码块、图片或者嵌套很深的引用块，仍然交给浏览器决定如何断栏。

> [!note]  
> 如果是纯文本小说，已经完全够用。对我来说肯定是不够用的。

### 分页组件太长，很难保证不屎山

`Mobile/PagedReader.vue` 已经超过两千行。里面同时负责尺寸测量、脚注拆分、手势、键盘、章节切换、阅读位置和空白页恢复。后面应该把测量调度、脚注布局和手势处理拆成单独的 composable，不然下次修分页时，我可能得先给自己写一份阅读器代码阅读器。

---

结果看来，中文的排版加上自适应屏幕的分页机制确实不简单。经典前端喜欢造轮子，T 叔叔送的额度重置不就得使劲蹬啊。就爱整 CSS 多栏布局、多轮 DOM 测量、按页脚注和若干 CJK 排版保险嗷。

网络上肯定早就有现成的分页阅读器可以使用。我为什么不用呢？

是啊，为什么 {.signature}
