---
title: 分页阅读器 2.0
aliases:
  - 网站更新记录
  - 阅读器重构
  - Markdown 与排版
date: 2026-08-23T00:00:00.000Z
tags:
  - 开发/记录
banner: reader.jpg
created: 2026-08-24T12:08:37.000Z
modified: 2026-08-29T08:18:33.000Z
public: true
---

## 组件与函数抽象

在上一篇推文《[Web 实现分页阅读器](/blog/2026-08-11)》里，我提到 `Mobile/PagedReader.vue` 已经超过两千行，后面应该把一些逻辑拆成单独的 composable。

> [!chat]
>
> > **GPT-5.6** 2026/08/23    
> > 哦，先生，抽象这些得消耗不少 token。
>
> > **Mori** 2026/08/23  
> > token 给我消耗好了啊喂！
> 
> > **Mori** 2026/08/23  
> > (╬▔皿▔)╯我说拆出测量调度、脚注布局和手势处理等逻辑你就只给我拆这三个是吧
> 
> > **GPT-5.6** 2026/08/23  
> > 收到。
> 
> > **GPT-5.6** 2026/08/23  
> > “等逻辑” ≠ “就这三个逻辑”。
> 
> > **GPT-5.6** 2026/08/23  
> > 我刚才成功展示了什么叫**严格满足示例，完美违背意图**。
> 
> > **GPT-5.6** 2026/08/23  
> > 这锅算我的。token 预算既然已经获得甲方 Mori 批准，我就不再进行这种毫无意义的节能模式了。
> 
> > **GPT-5.6** 2026/08/23  
> > 这次把整个文件的职责全部过一遍，该拆的都拆。
>
> > **Mori** 2026/08/23  
> > →_→ 说得好，扣 1 能给我重置额度吗？
> 
> > **Mori** 2026/08/23  
> > 111111

如此一来，原组件竟然只从提交时的两千五百多行缩到现在的一千七百多行。剩下的页面排版对齐还和一堆杂七杂八的东西挤在一起。反正我现在学乖了，不要让 AI 一直在同一个文件上不断堆积屎山，最后是我没心力看、它没 token 查，浪费额度这一块。

那让我们来看看它抽象了什么：

```js
import { usePagedChapterTransition } from
  "@/composables/novel/usePagedChapterTransition";
import { usePagedFootnoteLayout } from
  "@/composables/novel/usePagedFootnoteLayout";
import { usePagedReaderInput } from
  "@/composables/novel/usePagedReaderInput";
import { usePagedReadingPosition } from
  "@/composables/novel/usePagedReadingPosition";
import { usePaginationKeyboardFreeze } from
  "@/composables/novel/usePaginationKeyboardFreeze";
import { usePaginationObservers } from
  "@/composables/novel/usePaginationObservers";
import { usePaginationScheduler } from
  "@/composables/novel/usePaginationScheduler";
```

- `usePaginationScheduler` 管理分页任务的排队、失效和重新测量；
- `usePagedFootnoteLayout` 处理脚注所在页、续段和脚注高度；
- `usePagedReaderInput` 接管点击区域、滑动、滚轮和音量键输入；
- `usePagedChapterTransition` 管理跨章节时的页面快照与切换方向；
- `usePagedReadingPosition` 保存页码、锚点和页面内偏移；
- `usePaginationKeyboardFreeze` 避免软键盘弹出时反复重排；
- `usePaginationObservers` 统一观察正文、字体和容器尺寸变化。

> [!note]  
> 其实这个阅读器的正文基线对齐一直没有修好，我真没招了，直接推翻重来说不定会快点。

## 操作逻辑和界面优化

![[reader-2.0.png|阅读器 2.0]]

目前的阅读器有着一套相对完整的控制系统：

- 左右分页与上下滚动两种阅读模式；
- 页码跳转与全书阅读进度；
- 书内搜索与搜索历史替换导航；
- 章节目录、返回封面和刷新内容；
- 字体、字号、行高、段间距和背景颜色自定义；
- 阅读布局预设的保存、应用、删除和重置；
- 可自定义的九宫格快捷点击区域；
- 滚轮、左右滑动、点击区域和不完全支持的音量键翻页。

阅读位置也不再只保存一个含糊的百分比。

当前实现会记录 `anchorToken`（当前阅读的内容块）、`contextId`（内容归属）以及 `viewportTop`（锚点与视口顶部的距离）。不同断点还会分别保存状态，避免手机分页位置直接拿去恢复桌面滚动位置。恢复时，阅读器先找到锚点，再按照保存的视口偏移进行校正；如果字体加载或布局变化继续推动正文，还会重新检查位置。

## 解耦阅读器与 Markdown 渲染

过去，博客、桌面小说和移动小说对 Markdown 的处理多少有些各自为政。目前，我把提示块、聊天记录、朋友圈和代码块迁移成统一的 Markdown Vue 组件以方便调整功能与排版：

```
Markdown
├─ Alert.vue
├─ Chat.vue
├─ Moment.vue
├─ CodeBlock.vue
└─ RenderedContent.vue
```

服务端预渲染时，Markdown 插件先输出结构稳定的 HTML；浏览器接管以后，再把需要交互的部分挂载成 Vue 组件。这样既不能让 SSG 页面只剩一个自定义标签空壳，也不能让客户端挂载后突然换成另一棵 DOM，触发 hydration mismatch。

代码块的改动比较大：

- 保留服务端可直接阅读的代码内容；
- 客户端挂载复制按钮和语言信息；
- 行号与正文共用一个横向滚动容器；
- Highlight.js 的颜色改用主题变量；
- 剪贴板 API 不可用时仍保留旧式复制回退。

## 段评功能升级

原来的段评主要依靠 Markdown 段落编号：`{内容 UUID}-{段落序号}`。但文章里可以评论的显然不只有 `<p>`。所以，现在的段评插件将在渲染时维护计数器，为可评论节点写入：`data-reader-paragraph-id` 和 `data-source-type`。评论按钮则抽成统一的 `CommentTrigger`。有评论时显示数量，没有评论时保持隐藏；鼠标、触摸和键盘最终都走同一套打开事件。与此同时，桌面端和移动端的文本选择菜单也统一了。选中文字以后，可以：

- 复制文本；
- 在全文中搜索；
- 打开所在段落的段评；
- 生成分享卡片——这个坑留到下篇再说。

选择上下文不再只依靠 `Selection.toString()`。代码还会确认选区属于哪一个可评论块、对应什么段落 ID，并排除脚注回链、评论按钮等不属于正文的内容。

## 字体引入调整

我把原来的字体入口拆成了几部分：

```
font/
├─ result.css
├─ latin.css
├─ cjk.css
└─ sinhala.css
```

其中：

- `result.css` 负责统一字体变量与远端字体入口；
- `latin.css` 管理 Petrona、Inter、JetBrains Mono 等西文字体；
- `cjk.css` 管理中文字体；
- `sinhala.css` 管理僧伽罗文字体。

西文正文无衬线字体换用与 Noto Sans SC 更搭的 Inter，衬线场景则换用了与 Noto Serif SC 更搭的 Petrona。中文手写字体增加了志莽行书；代码继续使用 JetBrains Mono。

此次字体引入调整还增加了斜体的更纱黑体 SC。浏览器在缺少真正斜体字形时，经常直接把正体机械倾斜。为了让中西文混排中的强调文本尽量一致，当前实现只把实际存在的更纱黑体斜体切片别名到对应字族，并保留各自的 `unicode-range`。

## 总结

- 分页器从巨型组件变成中巨型组件加若干 composable；
- 桌面端与移动端共享阅读控制和 Markdown 组件；
- 阅读位置从简单段落 ID 变成锚点与视口偏移；
- 段评从普通段落扩展到标题和列表；
- 站点主题、代码高亮与评论区主题开始同步；
- 字体加载按语言和用途重新整理。

> [!chat]
> 
> > **GPT-5.6** 2026/08/23    
> > 这些更新不太像某个一眼可见的新按钮，更像把房子里的水管、电线和承重墙重新排了一遍。
> 
> > **GPT-5.6** 2026/08/23  
> > 外面看起来还是那个网站。
>
> > **GPT-5.6** 2026/08/23  
> > 只是下次再打孔时，大概不会立刻把整栋楼停电。
>
> > **Mori** 2026/08/23  
> > 有没有人告诉你你的比喻很烂 o_O ....

---

## 相关提交

- [`9518e8e`](https://github.com/KoMoriSam/MoriWebsite/commit/9518e8e)：重构移动端阅读控制与排版自定义
- [`c0b1a7f`](https://github.com/KoMoriSam/MoriWebsite/commit/c0b1a7f)：继续解耦阅读器与 Markdown 渲染
- [`932b2cf`](https://github.com/KoMoriSam/MoriWebsite/commit/932b2cf)：扩展站点主题并支持跟随系统
- [`c690d87`](https://github.com/KoMoriSam/MoriWebsite/commit/c690d87)：重构代码块与主题高亮
- [`f79d997`](https://github.com/KoMoriSam/MoriWebsite/commit/f79d997)：扩展标题、列表和章节头段评
- [`127b7a7`](https://github.com/KoMoriSam/MoriWebsite/commit/127b7a7)：统一跨端文本选择菜单
- [`a837987`](https://github.com/KoMoriSam/MoriWebsite/commit/a837987)：重构多语言字体体系
