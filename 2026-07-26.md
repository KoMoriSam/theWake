---
title: 从 SPA 到 SSG——记本站的不完全 SEO 优化
aliases:
  - SSG
  - ViteSSG
  - 预渲染
  - SEO
date: 2026-07-26T00:00:00.000Z
tags:
  - 开发/记录
banner: coding.png
created: 2026-07-26T11:43:20.000Z
modified: 2026-08-02T19:57:28.000Z
public: true
---

好消息！现在爬虫来爬取一篇博客文章，是能在 HTML 源码里看到真正的标题、摘要和正文了。

w(ﾟДﾟ)w 原来竟然不能！{.signature}

坏消息！！「关于装上 ViteSSG 依赖来帮助网站进行预渲染好像让事情变得很复杂这件事～」

(；´д｀) ゞ 你这家伙，Bug 可是越写越多的啊喂！{.signature}

---

## 为什么要使用 SSG？

在谈为什么之前，先来看看什么是 SSG 和 SEO[^1]：

> [!tip] 静态站点生成器（Static site generator, SSG）  
> **SSG** 是一种用于生成静态网站的软件。静态网站由 [HTML](https://developer.mozilla.org/zh-CN/docs/Glossary/HTML)、[CSS](https://developer.mozilla.org/zh-CN/docs/Glossary/CSS) 和 [JavaScript](https://developer.mozilla.org/zh-CN/docs/Glossary/JavaScript) 文件组成。最重要的是，静态站点不存在[服务器端逻辑](https://developer.mozilla.org/zh-CN/docs/Learn_web_development/Extensions/Server-side)，因此对于任何给定的 URL，所有用户都会收到相同的内容。作者可以用生成器支持的任意格式编写内容，如 Markdown、reStructuredText、HTML（有时甚至包括 [React](https://developer.mozilla.org/zh-CN/docs/Learn_web_development/Core/Frameworks_libraries/React_getting_started) 等），而生成器会将它们编译为一组优化的静态文件，供浏览器渲染。
> 
> 静态网站常用于博客、文档以及其他以内容驱动的网站，这些网站不需要在服务器端获取或生成数据。由于可以通过 [CDN](https://developer.mozilla.org/zh-CN/docs/Glossary/CDN) 进行分发，它们具有速度快、安全性高，且更易于部署的优点。

> [!tip] 搜索引擎优化（Search Engine Optimization, SEO）  
> **SEO** 是指通过优化网站，使其在搜索结果中更加显眼，即提升搜索排名的过程。
> 
> 搜索引擎通过 [爬取](https://developer.mozilla.org/zh-CN/docs/Glossary/Crawler) 网页，在页面之间跟随链接，并对发现的内容进行索引。当你进行搜索时，搜索引擎会展示已索引的内容。爬虫遵循特定规则。如果在进行网站 SEO 时严格遵循这些规则，就能让网站有最大机会出现在搜索结果的前列，从而提升流量，并可能增加收入（对电子商务和广告而言）。
> 
> 搜索引擎会提供一些 SEO 指导方针，但大型搜索引擎将结果排名视为商业机密。SEO 结合了官方搜索引擎指南、经验知识以及来自科学论文或专利的理论知识。
> 
> SEO 方法大致分为三大类：
> 
> **[技术层面](https://developer.mozilla.org/zh-CN/docs/Glossary/SEO#%E6%8A%80%E6%9C%AF)**：使用语义化 [HTML](https://developer.mozilla.org/zh-CN/docs/Glossary/HTML) 对内容进行标记。在爬虫浏览网站时，应只让它们发现你希望被索引的内容。
> 
> **[文案写作](https://developer.mozilla.org/zh-CN/docs/Glossary/SEO#%E6%92%B0%E7%A8%BF)**：使用访问者的语言来撰写内容。同时运用文字和图片，以便爬虫理解主题。
> 
> **[热度提升](https://developer.mozilla.org/zh-CN/docs/Glossary/SEO#%E5%A3%B0%E6%9C%9B)**：当其他成熟网站链接到你的网站时，你将获得最多的流量。

升级 SSG 前，本站还是一个单页应用（Single-page application），也就是我们常说的 SPA。Vite 负责打包。浏览器加载 JavaScript，Vue 启动后请求文章列表和 Markdown 文件，再把内容渲染到页面上。

对正常打开网站的人来说，这套流程没什么问题。但在 JavaScript 执行之前，文章页面的 HTML 基本只是一个空壳。也就是说，我确实写了文章，但对于无法执行 JavaScript 的搜索引擎和爬虫（至少大多都无法执行）来说，能看到的网站主体长这样：

```html
<div id="app"></div>
```

根本没有内容，这显然不适合 SEO。

综上，本站要做好 SEO 其实是具有天然缺陷的。这也反应出为什么使用 Vue 的项目很大部分都是后台管理系统。

---

## 升级前的方案

最开始我使用的是 `vite-plugin-seo-prerender`，它的逻辑是项目构建完成后启动一个无头浏览器，逐个访问配置好的路由，再把浏览器最终生成的 HTML 保存下来。

在没有博客和小说的时候，这套方案没什么问题。只要给 Puppeteer 指定浏览器路径，再填上几个主要页面的路由地址，构建完成后就能拿到预渲染结果。因为预渲染不需要从仓库获取我的文章，没有异步过程，不需要真实用户来执行，直接生成即可。当我需要前后端分离来方便我的写作时，这个问题就大了——无头浏览器可不会主动去等待文章读取，并且路由的手动配置在面对大量文章的路由时也显得十分麻烦。

于是，我在把网站托管从 GitHub 换到 Cloudflare 的同时，想着把这个长期以来的半吊子预渲染也一并解决。

本来我以为沿用之前的插件配置即可，在本地构建并上传云端都没什么问题，想当然的以为这次搬迁能够一步成功。可是在云端构建的几次失败后，我发现配置里还留着这么一段：

```js
executablePath:
  "C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe"
```

接下来又是判断 CI 环境、移除本地 Chrome 路径，又是添加 `--no-sandbox` 参数，调整构建流程……折腾一圈，还是绕不开这套方案本身的问题。构建时需要下载 Chrome 依赖、启动浏览器，页面里的异步请求根本不会响应。

> [!note]  
> 浏览器预渲染本身没有什么问题。只是对这个依赖远端文章数据、又部署在 Cloudflare 上的小站来说，它额外带来了一套需要维护的浏览器运行环境。

于是 `vite-ssg` 堂堂登场。

---

## 新的方案

切换到 `vite-ssg` 后，应用入口从普通的 `createApp` 改成了 `ViteSSG`：

```js
export const createApp = ViteSSG(
  App,
  {
    routes,
    base: import.meta.env.BASE_URL,
  },
  ({ app }) => {
    app.use(createPinia());
  },
);
```

但只改入口并不能解决预渲染问题，说到底现在只是把一个预渲染插件换成了另一个。

真正麻烦的是，我的文章、小说和网站代码根本不在同一个仓库里。平时由浏览器打开页面时，可以分别请求这些数据。现在换成 SSG，构建阶段就得提前知道：

1. 文章数量；
2. 每篇文章的路径；
3. frontmatter 内容；
4. Markdown 正文；

那就在 `build` 前加一个 `prebuild` 吧——正式构建之前，脚本会读取生产环境配置，获取文章索引，再逐篇下载 Markdown、解析 frontmatter、处理头图地址，等等。最后根据这些内容生成一份 `ssg-data.generated.js`，里面包含：

- 博客文章的元数据和正文；
- 小说卷目与章节目录；
- 网站更新日志；
- 每篇内容对应的静态路径。

之后，路由会根据这些数据展开：

```text
/blog/2026-06-25
/blog/2025-09-09
/blog/2024-07-07
……
```

这样一来，每篇文章都会在构建阶段变成一条确定的路由，不再等到运行时才解析 `:articleId`。

---

## 最麻烦的部分

生成了 HTML，事情还没结束。浏览器拿到页面后，Vue 还要在现有 HTML 上恢复响应式状态和事件监听。这个过程就是 hydration[^2]。

假如服务端生成的页面里已经有文章正文，浏览器启动 Vue 时拿到的初始数据却还是空数组，Vue 就会发现你给我的页面，和我准备接管的页面，好像不是同一个东西？然后报出 hydration mismatch。解决办法不是让服务端和浏览器各自请求一次数据，而是让两边读取同一份构建期快照。

现在，文章元数据和正文会被写进静态路由的 `meta`。文章页面初始化时，直接使用这份数据。等浏览器完成挂载后，再去远端请求最新内容。如果页面上已经有 SSG 生成的正文，就保留当前内容，不重新切回加载状态。

写 SPA 时，几乎可以默认所有代码都运行在浏览器里。而在 SSG 中则要考虑到很多开发限制，浏览器特定的代码只能在特定的生命周期钩子中使用；某些外部库可能需要特殊处理才能在服务器端渲染的应用程序中运行。例如，代码里那些默认 `window`、`document`、`localStorage` 和 `navigator` 一定存在的地方，也都需要增加环境判断。主题设置之类只能在浏览器运行的逻辑，则需要用 `<client-only>` 包起来。

---

## 从能够预渲染到拥有 SEO 信息

目前只是解决了每篇文章页面的预渲染问题。因此为了更好的 SEO，我又整理了一遍数据来源，并补上网页信息和搜索相关内容。现在每篇静态文章页都包含：

- 独立的 `<title>` 和摘要；
- canonical 链接；
- Open Graph 和 Twitter Card 信息；
- 发布时间和修改时间；
- `BlogPosting` 结构化数据；
- 已经渲染完成的 Markdown 正文。

之后，构建流程初步加入了 Pagefind。它会扫描 `dist` 目录，根据构建后的静态页面生成本地全文索引。不过，现在全局搜索的逻辑仍然是由浏览器获取博客、小说和更新日志内容，再放进内存里检索，Pagefind 并没有接入。

写下这篇文章时，一次完整的构建会生成 13 个页面，其中有 8 篇独立文章；Pagefind 也成功索引了这 8 篇文章。

现在即使搜索引擎不执行 JavaScript，也能直接读到文章内容，留下的 SEO flag 姑且可以勾掉一半了。

---

## 现在还有哪些问题？

### 构建过程仍然依赖远端内容源

目前每次构建都要访问 `theWake` 和 `theHorizon`，并且是逐篇获取（文章数量少的情况下，还算能够接受）。文章源、网络、域名和构建环境里，任何一环出了问题，都可能导致构建失败。下载文章失败时，脚本也只会跳过对应文件。虽说这样至少能让其他页面继续生成，但某些文章可能悄悄漏掉，而我不能及时发现，以后内容多起来，缓存、并发请求、失败重试和完整性检查都得补上。

### SSG 快照目前整体进入客户端主包

为了保证 hydration 前后的数据一致，现在所有文章正文、文章元数据、小说目录和更新日志都会写进同一个生成文件，再一起进入客户端代码。这意味着用户只是打开主页，也可能提前加载根本用不到的文章正文。同一份正文还会同时出现在文章 HTML 和 Pagefind 索引里。

目前所构建的现代浏览器主包大约是 `342 KiB`，gzip 后约 `132 KiB`。这个体积不全是 SSG 带来的，但把整座图书馆塞进每个用户的背包里，显然不能一直这么干。之后应该把列表数据和正文拆开，让每个静态页面只携带自己需要的内容。

### 静态内容和线上最新内容可能存在两个版本

现在文章页面会先显示构建时生成的静态快照，浏览器挂载后再请求线上内容。这样一来，内容源更新后，读者不必等网站重新部署就能看到新版本。但问题也跟着来了：读者看到的文章可能已经更新，搜索引擎、分享摘要和 Pagefind 索引却还停留在上一次部署。之后可以让内容仓库更新时自动触发网站构建，或者干脆规定静态页面就是最终发布版本，别让同一篇文章同时存在两套内容。

### 小说还没有真正进入逐章 SSG，Pagefind 也还没接入

目前统一快照里只有小说目录，没有每章正文。

> [!tip] P.S.  
> 当然，按照我的性格和目前的想法，我其实不太愿意让小说被爬虫偷走，再公之于搜索引擎（羞死了羞死了）。

这次 Pagefind 也只索引了 8 篇博客文章，没有包含小说章节。刚才也提到，生成出来的索引目前还没有被搜索页面使用，全局搜索仍然需要浏览器逐篇获取文章和小说内容，再进行检索。

### SSG 不等于性能优化

SSG 只解决了首屏内容和搜索引擎读取的问题。JavaScript、CSS、字体和图标资源并没有减，目前构建时仍然会出现大 chunk 警告，旧浏览器兼容包超过 `1 MiB`；Markdown 渲染、中文字体和图标资源也占了不少体积。

---

## 接下来的计划

- [ ] 拆分文章列表数据和正文 payload，避免所有内容进入主包；
- [ ] 为远端内容获取增加并发、缓存、重试和完整性校验；
- [ ] 让 `theWake`、`theHorizon` 的更新自动触发网站构建；
- [x] [`651e549`](https://github.com/KoMoriSam/MoriWebsite/commit/651e54970d52cfa73ae2d7e0f02623f1d3eeffb5): 将 Pagefind 接入全局搜索；
- [ ] 恢复 SSG 产物和 hydration 的自动检查；
- [ ] 继续处理主包、样式、字体和旧浏览器兼容带来的体积问题。

---

## 相关提交

不想看我描述过程，只想直接看代码变化的话，可以从这些提交开始：

- [`9a49faf`](https://github.com/KoMoriSam/MoriWebsite/commit/9a49faf)：更新版本号至 1.13.0
- [`a8ac7c9`](https://github.com/KoMoriSam/MoriWebsite/commit/a8ac7c9)：针对 Cloudflare 构建修改预渲染实现
- [`aab5af1`](https://github.com/KoMoriSam/MoriWebsite/commit/aab5af1)：换用 ViteSSG 进行 SEO 预渲染
- [`db5b650`](https://github.com/KoMoriSam/MoriWebsite/commit/db5b650)：重构并合并 SSG 数据来源
- [`6b0d91d`](https://github.com/KoMoriSam/MoriWebsite/commit/6b0d91d)：加入 Pagefind 页面索引
- [`7a100a5`](https://github.com/KoMoriSam/MoriWebsite/commit/7a100a5)：补充页面 SEO 信息与 404 处理
- [`3af74f5`](https://github.com/KoMoriSam/MoriWebsite/commit/3af74f5)：继续优化 SSG 页面 SEO

现在，本站已经可以为每篇博客文章生成独立的静态 HTML。现在它有着更快的内容加载速度、更好的搜索引擎优化，等等。不过数据来源、搜索、性能和自动检查这几块，都还留着不少问题。嗯……就先这样吧。

[^1]: 本文中关于 SSG 和 SEO 的定义参考了 [MDN Web 术语表](https://developer.mozilla.org/en-US/docs/Glossary)。

[^2]: 关于 Hydration，详见 <https://en.wikipedia.org/wiki/Hydration_(web_development)>。
